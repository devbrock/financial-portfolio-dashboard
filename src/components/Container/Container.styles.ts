import { cva } from 'class-variance-authority';

/**
 * Spec: `design_system.json` -> components.primitives.Container.classes.base
 */
export const containerStyles = cva('mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8');
